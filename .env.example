# ====================================
# Goodreads → StoryGraph Sync Service
# ====================================
# Copy this file to .env and fill in your credentials
# DO NOT commit the .env file - it contains sensitive information!

# ====================================
# Multi-Account Support
# ====================================
# NEW: You can now sync multiple accounts!
#
# Option 1 (RECOMMENDED): Multi-Account Mode
# - Copy accounts.example.json to /data/config/accounts.json
# - Add all accounts there (supports unlimited accounts)
# - Credentials below will be IGNORED if accounts.json exists
#
# Option 2: Single-Account Mode (Legacy)
# - Use the credentials below for a single account
# - This works as before - no changes needed for existing users
#
# Each account gets:
# - Separate state tracking (independent sync history)
# - Separate browser sessions (no login conflicts)
# - Separate export artifacts
# - Individual error handling (one failure doesn't stop others)

# ====================================
# Required Credentials (Single-Account Mode Only)
# ====================================
# These are only used if /data/config/accounts.json doesn't exist

# Goodreads account credentials
GOODREADS_EMAIL=your-email@example.com
GOODREADS_PASSWORD=your-goodreads-password

# StoryGraph account credentials
STORYGRAPH_EMAIL=your-email@example.com
STORYGRAPH_PASSWORD=your-storygraph-password

# ====================================
# Scheduling Configuration
# ====================================

# Cron schedule for automated syncs (default: 3 AM daily)
# Format: minute hour day month weekday
# Examples:
#   0 3 * * *     - Every day at 3 AM
#   0 */6 * * *   - Every 6 hours
#   0 0 * * 0     - Every Sunday at midnight
CRON_SCHEDULE=0 3 * * *

# Timezone for scheduling (default: America/New_York)
# Use standard timezone names (e.g., America/Los_Angeles, Europe/London, UTC)
TZ=America/New_York

# ====================================
# Sync Behavior
# ====================================

# Run in headless mode (no visible browser window)
# Set to false for debugging/troubleshooting
HEADLESS=true

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Maximum number of books to sync (for testing)
# Leave empty or comment out for no limit
# MAX_SYNC_ITEMS=100

# Dry run mode - export from Goodreads but don't upload to StoryGraph
# Useful for testing without affecting your StoryGraph library
DRY_RUN=false

# Force full sync even if CSV hasn't changed
# Normally, unchanged libraries skip upload (smart sync)
FORCE_FULL_SYNC=false

# ====================================
# Advanced Configuration
# ====================================

# Browser timeout in milliseconds (default: 30000)
# Increase if you have a slow internet connection
# BROWSER_TIMEOUT=30000

# Storage state expiry in days (default: 30)
# How long to reuse browser sessions before forcing re-login
# STORAGE_STATE_EXPIRY_DAYS=30
